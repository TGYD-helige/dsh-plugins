# Inserts the dsh-langfuse plugin row; plugin config is the Config schema in
# src/index.ts (overridable in the profile's own cordis.patch.yml).
- insert:
    - id: langfuse
      name: '@amaster.ai/dsh-langfuse'
